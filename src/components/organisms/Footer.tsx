import { FC } from "react";

const Footer: FC = () => {
  return (
    <footer className="px-10 py-10 relative ">
      <div className="flex flex-col md:flex-row items-center gap-6"></div>
    </footer>
  );
};

export default Footer;
